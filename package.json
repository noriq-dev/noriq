{
  "name": "noriq",
  "private": true,
  "version": "0.1.0",
  "description": "AI-native project management — MCP coordination layer for agents, web app for humans",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @noriq/api",
    "dev:web": "npm run dev --workspace @noriq/web",
    "build": "npm run build --workspace @noriq/shared && npm run build --workspace @noriq/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "deploy": "npm run build && npm run deploy --workspace @noriq/api"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.25.4": true,
    "esbuild@0.28.1": true,
    "workerd@1.20250906.0": true,
    "workerd@1.20260708.1": true,
    "sharp@0.33.5": true,
    "sharp@0.34.5": true
  }
}
